<div class="wrapper top fadeInDown">
<div id="formContent" class="large">
  <div class="container">
    <div class="row">
        <div class="col s12">
          <ul class="tabs">
            <% for (var i = 0; i < cantDias; i++) { %>
                <li class="tab col s3"><a  class="active" href="#test1">Día <%= i+1 %></a></li>
            <% } %>
          </ul>
        </div>
      </div>
      <div class="row">
      <% _.each(evento.momentos, function(momento) { %>
          <div id="test1" class="col s12"><%= momento.nombre %></div>
      <% }) %>
      </div>
  </div>
</div>
</div>
